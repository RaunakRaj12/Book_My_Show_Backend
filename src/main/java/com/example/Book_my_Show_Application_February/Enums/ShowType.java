package com.example.Book_my_Show_Application_February.Enums;

public enum ShowType {

    _3D , _2D

    // Because enums can't start with a number
    // that's why we have written with the underscore(_)
}
